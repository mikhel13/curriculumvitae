import React from 'react';

const Sidebar = ({ children }) => (
    <div className="Sidebar">
        <div className="Sideabr-container">
            { children}
        </div>
    </div>

);

export default Sidebar;